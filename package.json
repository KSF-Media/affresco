{
  "private": true,
  "name": "stor",
  "dependencies": {
    "lerna": "^3.3.2",
    "parcel-bundler": "^1.10.3",
    "psc-package-bin-simple": "^1.0.1",
    "purp-bin-simple": "^1.0.1",
    "purescript": "^0.12.0"
  },
  "bootstrap": {
    "useWorkspaces": true,
    "npmClient": "yarn"
  },
  "scripts": {
    "build-purs": "psc-package build \"packages/*/src/**/*.purs\" && ./build-purs.rb",
    "clean": "rm -rf .cache .psc-package .psci_modules node_modules output dist"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
