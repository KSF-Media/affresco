{
  "private": true,
  "name": "affresco",
  "dependencies": {
    "parcel-bundler": "^1.10.3",
    "purescript": "^0.13.6",
    "spago": "^0.9.0",
    "yarn": "^1.22.0",
    "uglify-js": "^3.6.0"
  },
  "bootstrap": {
    "useWorkspaces": true,
    "npmClient": "yarn"
  },
  "scripts": {
    "clean": "rm -rf .cache .psci_modules node_modules output dist",
    "copy-hooks": "cp hooks/* .git/hooks"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "yarn": "^1.13.0"
  }
}
