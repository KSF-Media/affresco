{
  "name": "@affresco/app-article",
  "version": "0.1.0",
  "dependencies": {
    "@ksf-media/user": "file:../../packages/user",
    "@sentry/browser": "^6.19.7",
    "@sentry/tracing": "^6.19.7",
    "basscss": "^8.1.0",
    "bootstrap-css-only": "^4.3.1",
    "js-cookie": "^2.2.1",
    "react": "^16.8.3",
    "react-device-detect": "^1.6.2",
    "react-dom": "^16.8.3",
    "react-image-lightbox": "^5.1.0",
    "uuid": "^8.3.2"
  },
  "scripts": {
    "start": "parcel src/index.html -p 8000",
    "build-user": "npm --prefix '../../packages/user' run build-npm",
    "build": "npm run build-user && npm run clean && npm ci && parcel build src/index.html --public-url ./ --no-source-maps && cp src/xd_receiver.html ./dist && cp src/ads.txt ./dist",
    "clean": "rm -rf .cache dist",
    "test": "echo 'No testsuite configured...'"
  },
  "devDependencies": {
    "@babel/core": "^7.18.2",
    "parcel-bundler": "^1.12.3"
  }
}
